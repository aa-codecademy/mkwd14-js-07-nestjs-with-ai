import primitives from './01-primitives';
import arrays from './02-arrays';
import runExamplesForMethodDecorator from './08-decorators-method';

// Entry file for the first TypeScript class.
// We call one demo function here and can swap this import later
// to run the other lesson files during class.
// primitives();
// arrays();
runExamplesForMethodDecorator();
